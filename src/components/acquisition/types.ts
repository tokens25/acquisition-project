/**
 * The Figma `Device` variant shared by every Acquisition component.
 * mobile → compact type scale · desktop → default · xl → the "Extra big" breakpoint.
 */
export type Device = 'mobile' | 'desktop' | 'xl'
